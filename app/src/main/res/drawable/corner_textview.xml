<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 设置透明背景色 -->
    <solid android:color="#fff" />

    <!-- 设置一个黑色边框 -->
    <!--<stroke
        android:width="2px"
        android:color="@color/lowgray" />-->
    <!-- 设置四个圆角的半径 -->
    <corners
       android:radius="8dp"
        />
    <!-- 设置一下边距,让空间大一点 -->
    <padding
        android:bottom="8dp"
        android:left="8dp"
        android:right="8dp"
        android:top="8dp" />

</shape>
